node_modules/
package-lock.json

# The MegaMUD database this is built from: 12 MB of third-party binary that
# does not belong in source control. ./init.sh fetches it and regenerates
# data/gamedata.js, which is committed.
*.mdb

# created by init.sh
.venv/
__pycache__/
